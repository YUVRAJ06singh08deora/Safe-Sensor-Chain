// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#27ae60', // Green color
    },
    secondary: {
      main: '#1a1a1a', // Black color
    },
  },
});

export default theme;
